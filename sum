#include<stdio.h>
void main()
{
 int n,sum=0,i,pass;
 printf("enter the number of elements");
 scanf("%d",&n);
 for(i=0;i<=n;i++)
 {
   if(i%3==0)
   {
    sum=sum+i;
    }
   else
   {
     pass;
    }
  }
 printf("sum is %d",sum);
}

